# Legacy signaling server

This Node WebSocket signaling server is **deprecated**.

Realtime membership, rooms, chat, and WebRTC signals now use **Firebase** (Firestore + Realtime Database for presence). Electron no longer starts this process on boot.

Kept for reference / emergency LAN experiments only:

```bash
npm run signaling
```

Do not treat this folder as the production backend.
